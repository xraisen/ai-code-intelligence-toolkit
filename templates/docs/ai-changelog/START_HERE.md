# AI Changelog Start Here

This folder is durable project memory for AI coding agents.

Read this file before reopening old bug areas, reverting fixes, or touching code that may already have been repaired.

## Required anti-drift order

1. Read `AGENTS.md`.
2. Read `docs/ai-changelog/START_HERE.md`.
3. Check `docs/ai-changelog/history.index.json`.
4. Run `npm run typedoc:json:local && npm run ai:graph:build`.
5. Run `npm run ai:spec -- "<task>"`.
6. Run `npm run ai:preflight -- "<task>"`.
7. Run `npm run ai:graph:query -- "<specific symbol/file/error/feature>"`.

## Machine-readable summary

```json
{
  "schemaVersion": 1,
  "totalEntries": 0,
  "latest": []
}
```

## Entry index

| # | File | Task | Status | Updated |
|---:|---|---|---|---|
| - | - | - | - | - |
